SupercellSet
============

.. currentmodule:: libcasm.configuration

.. autoclass:: SupercellSet
  :show-inheritance:

  
  
  .. rubric:: Methods

  .. autosummary::
    :nosignatures:
    :toctree:
    :template: custom-function-template.rst
  
    ~SupercellSet.add
    ~SupercellSet.add_by_canonical_name
    ~SupercellSet.add_by_transformation_matrix_to_super
    ~SupercellSet.add_record
    ~SupercellSet.add_supercell
    ~SupercellSet.clear
    ~SupercellSet.discard
    ~SupercellSet.discard_by_canonical_name
    ~SupercellSet.discard_by_transformation_matrix_to_super
    ~SupercellSet.discard_record
    ~SupercellSet.discard_supercell
    ~SupercellSet.empty
    ~SupercellSet.from_dict
    ~SupercellSet.prim
    ~SupercellSet.remove
    ~SupercellSet.remove_by_canonical_name
    ~SupercellSet.remove_by_transformation_matrix_to_super
    ~SupercellSet.remove_record
    ~SupercellSet.remove_supercell
    ~SupercellSet.to_dict
  
  

  
  
    